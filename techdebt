-Move things out from datastructure.h that aren't datatstructures anymore like InputContainer
-Why does Weapon::beginstep exist?
-Dieing is broken in multiplayer
-src/renderer.cpp:    // TODO: ADD ANOTHER OPTIONS LIKE VSYNC
-src/ingameelements/map.cpp:    // TODO this thing
-Reinhardt shield position syncing needs to be done twice, once in step and once in endstep. This probably means something is broken if reinhardt is later than the shield.
-if configloader is given a nonexistent json path, it just hangs without error message. This needs to be fixed.
-controlpoint networking